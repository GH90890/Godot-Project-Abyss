extends KinematicBody2D
class_name Actor # makes it so we can extend it! ^look up but then with Actor

var FLOOR_NORMAL = Vector2.UP

export var speed: = Vector2(8.0, 10.0)
export var gravity: = 10.0  # this is a float value.

var velocity: = Vector2.ZERO# hey godot this is a vector 2
